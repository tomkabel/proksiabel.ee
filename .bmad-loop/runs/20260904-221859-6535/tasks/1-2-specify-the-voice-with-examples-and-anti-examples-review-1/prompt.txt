/bmad-build-auto /home/notroot/Documents/proksiabel.ee/_bmad-output/implementation-artifacts/spec-1-2-specify-the-voice-with-examples-and-anti-examples.md — do NOT modify, re-open, or rewrite existing deferred-work ledger entries; the orchestrator owns their status and resolution. sprint-status.yaml is owned by the orchestrator: never write it, and never revert a change to it. A row at done or awaiting-operator is the orchestrator's own bookkeeping — not a defect to fix, and not proof that the work is verified. If the story cannot be finished without a human decision, finalize the spec to status: blocked and say why. That is the hand-back channel; the board is not.
