/**
 * Source-controlled policy revision date shared by the legal pages.
 * Update when the cookie/privacy policy text changes; keeps the rendered
 * footer date stable instead of showing the build date.
 */
export const POLICY_REVISION_DATE = new Date(2026, 7, 11);
